//não precisa utilizar o else 

const prompt = require('prompt-sync')();

let velocidade = Number(prompt('qual a velocidade?:  '));
let conta1
conta1 = (velocidade - 80 )*7


if ( velocidade > 80 )(console.log(`você foi multado em R$ ${conta1}`))
    